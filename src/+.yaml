# Inline any code examples
inlined = mapDeep(content, inline, '.md'):

# Convert markdown to HTML while preserving front matter
html = mapDeep(inlined, mdHtml, '.md', '.html'):

# Apply the documentation page template
pages = mapDeep(html, templates/page.ori, '.html'):

# Give docs access to the API documentation based on the Origami source code
source = node_modules/@graphorigami/origami/src:
api = js/jsApi(source/core):

# Development site
develop = merge(client, pages, test, meta(this)):
  _redirects = src/_redirects:
  origamiApi = api:
  origamiSource = source:

# Production site
production = merge(client, pages, meta(this)):
  _redirects = src/_redirects:
