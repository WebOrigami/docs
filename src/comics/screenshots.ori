Tree.map(
  comicData.ori
  (comic) => Tree.map(comic/panels, {
    key: (panel) => panel.screenshot
    value: (panel) => screenshot.ori(panel.url)
  }
))

→ (screenshots) => Tree.cache(screenshots, files:../../cache)
