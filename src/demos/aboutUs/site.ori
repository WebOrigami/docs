{
  index.html = index.ori(teamData.yaml)

  team = @map(teamData.yaml, {
    keyMap: =`{{_/name}}.html`
    valueMap: person.ori
  })

  assets
  images

  thumbnails = @map(images, =@image/resize(_, { width: 400 }))
}