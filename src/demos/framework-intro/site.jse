{
  index.html = <index.jse>(<teamData.yaml>)

  team/ = Tree.map(<teamData.yaml>, {
    key: (person) => `${person.name}.html`
    value: <person.jse>
  })

  <assets>
  <images>

  thumbnails/ = Tree.map(<images>, (image) =>
    Origami.image.resize(image, { width: 400 })
  )
}
