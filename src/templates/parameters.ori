(parameters) => 
  Tree.map(parameters, (parameter) =>
    `${ parameter.optional ? "[" : "" }${ parameter.name }${ parameter.optional ? "]" : "" }`
  )

  → (texts) => Tree.values(texts).join(", ")
