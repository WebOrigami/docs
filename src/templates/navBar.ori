---
areas = (js%markCurrent)(parse(areas.yaml), currentArea):
currentArea = (js%pathArea)(path):
---
      <nav id="navBar">{{ map areas, =>`
        {{ templates/linkCurrent.ori() }}` }}
      </nav>