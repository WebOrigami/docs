<h2>{{_/name}} class</h2>
<p>
  {{ @if _/description, =`
    {{ @mdHtml _/description}}
  `}}
</p>
{{ @if _/classConstructor, =`
  {{ function.orit(_/classConstructor) }}
`}}
{{ @tree/map(_/members, =`
  {{ function.orit(_) }}
`) }}
