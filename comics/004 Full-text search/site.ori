{
  // Search page
  search.html

  // Search indexes generated on demand
  pagefind/ = package:@weborigami/pagefind({ posts })

  // The rest of your site…
  assets/
  index.html = index.ori(posts)
  posts/ = Tree.map(markdown/, Origami.mdHtml)
}
