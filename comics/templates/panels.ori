(comic) => Tree.map(comic.panels, {
  key: (value, key) => `${ key }.html`
  value: (panel) => page.ori.html(Tree.indent`
    <style>
      body {
        height: 600px;
        width: 800px;
      }
    </style>
    ${ <panel.ori>(panel, comic) }
  `)
})
