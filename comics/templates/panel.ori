// A single comic panel
(panel, index, comic) => Tree.indent`
  <comic-panel class="${ index % 2 === 0 ? 'even' : 'odd' }">
    <div class="caption">
      ${ panel.face ? `
          <div class="faceCircle">
            <div class="faceContainer">
              <img class="face" src="/assets/${ panel.actor.toLowerCase() }/${ panel.face }.svg" />
            </div>
          </div>
        ` : "" }
      <div class="bubble">
        <div class="tail"></div>
        <div class="speech">
          ${ Origami.mdHtml(panel.text) }
        </div>
      </div>
    </div>
${ .(panel.scene)(panel, comic) }
  ${ panel.footnote ?
    Tree.indent`
      <div class="footnoteContainer">
        <div class="footnote ${ panel.footnote.length > 60 ? "long" : "short" }">
          ${ Origami.mdHtml(panel.footnote) }
        </div>
      </div>
    ` : ""
  }
  </comic-panel>
`
