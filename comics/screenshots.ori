Tree.map(
  comics/
  (folder) => Tree.map(folder/comic.yaml/panels, {
    key: (panel) => panel.screenshot
    value: (panel) => screenshot.ori(panel.url)
  }
))

→ (screenshots) => Tree.cache(screenshots, files:cache/screenshots)
