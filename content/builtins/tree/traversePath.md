---
title: traversePath(tree, path)
supertitle: "Tree."
---

This traverses a [map-based tree](/async-tree/mapBasedTree.html) using the slash-separated keys in the string `path`. Example: Given the `path` “foo/bar”, this traverses the tree using the keys “foo/” and “bar”, then returns the resulting value.
