Tree.sort(
  flatMap.js(
    node_modules/@weborigami/origami/src/dev/help.yaml,
    (help, namespaceSlash) =>
      Tree.map(help.commands, (commandHelp, commandSlash) => {
        args: commandHelp.args
        description: commandHelp.description
        namespace: Origami.slash.remove(namespaceSlash)
        url: help.collection
          ? `/builtins/${ Origami.slash.remove(namespaceSlash).toLowerCase() }.html`
          : `/builtins/${ namespaceSlash.toLowerCase() }${ Origami.slash.remove(commandSlash) }.html`
      }
    )
  ),
  { compare: Origami.naturalOrder }
)
